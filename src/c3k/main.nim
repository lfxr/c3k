import
  types


func loadYaml*(filePath: string): SettingsYaml = 
  discard


func parseSettingsYaml*(settingsYaml: SettingsYaml): Settings =
  discard


func scan*(settings: string, fn: proc()) =
  discard
